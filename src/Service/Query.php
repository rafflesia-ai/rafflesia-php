<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Service;

use Rafflesia\Resource\EnvelopeQueryData;
use Rafflesia\Resource\EnvelopeQueryExplainData;

class Query
{
    public function __construct(
        private readonly \Rafflesia\HttpClient $client,
    ) {
    }

    /**
     * @param bool|null $allowUnverifiedJoins Explicit escape hatch for cross-database joins. When false, every cross-database join is strict and requires retained cardinality-consistent evidence.
     * @param \Rafflesia\Resource\QueryBudget|null $budget Optional explicit overrides bounded by the selected profile's published maximums.
     * @param \Rafflesia\Resource\QueryRequestBudgetProfile|null $budgetProfile Calibrated execution profile; defaults to interactive.
     * @param string|null $cursor
     * @param string|null $dialect Pinned portable SQL contract; omitted selects the current contract.
     * @param \Rafflesia\Resource\QueryRequestJoinEvidencePolicy|null $joinEvidencePolicy Automatically resolve and bind retained measurements for every complete declared SQL join. Cross-database joins default to cardinality_consistent unless allow_unverified_joins is true.
     * @param int|null $limit
     * @param string|null $ontologyDigest Optional exact ontology snapshot precondition. The request fails if this build cannot serve that digest.
     * @param array<\Rafflesia\Resource\DatabaseJoinEvidenceRequirement>|null $requiredJoinEvidence Optional exact retained join-measurement preconditions. Each row is verified against the named pinned sources before planning or execution.
     * @param array<\Rafflesia\Resource\QuerySource>|null $sources
     * @param string $sql
     * @param bool|null $strictJoins When true, coordinate-unsafe joins (unregistered pairs, missing keys, lossy or coordinate-range mappings, cross-release joins) fail the query instead of returning advisory warnings.
     * @return \Rafflesia\Resource\EnvelopeQueryData
     * @throws \Rafflesia\Exception\RafflesiaException
     */
    public function run(
        ?array $sources,
        string $sql,
        ?bool $allowUnverifiedJoins = null,
        ?\Rafflesia\Resource\QueryBudget $budget = null,
        ?\Rafflesia\Resource\QueryRequestBudgetProfile $budgetProfile = null,
        ?string $cursor = null,
        ?string $dialect = null,
        ?\Rafflesia\Resource\QueryRequestJoinEvidencePolicy $joinEvidencePolicy = null,
        ?int $limit = null,
        ?string $ontologyDigest = null,
        ?array $requiredJoinEvidence = null,
        ?bool $strictJoins = null,
        ?\Rafflesia\RequestOptions $options = null,
    ): \Rafflesia\Resource\EnvelopeQueryData {
        $body = array_filter([
            'allow_unverified_joins' => $allowUnverifiedJoins,
            'budget' => $budget,
            'budget_profile' => $budgetProfile?->value,
            'cursor' => $cursor,
            'dialect' => $dialect,
            'join_evidence_policy' => $joinEvidencePolicy?->value,
            'limit' => $limit,
            'ontology_digest' => $ontologyDigest,
            'required_join_evidence' => $requiredJoinEvidence,
            'sources' => $sources,
            'sql' => $sql,
            'strict_joins' => $strictJoins,
        ], fn ($v) => $v !== null);
        $response = $this->client->request(
            method: 'POST',
            path: 'v1/query',
            body: $body,
            options: $options,
        );
        return EnvelopeQueryData::fromArray($response);
    }

    /**
     * @param bool|null $allowUnverifiedJoins Explicit escape hatch for cross-database joins. When false, every cross-database join is strict and requires retained cardinality-consistent evidence.
     * @param \Rafflesia\Resource\QueryBudget|null $budget Optional explicit overrides bounded by the selected profile's published maximums.
     * @param \Rafflesia\Resource\QueryRequestBudgetProfile|null $budgetProfile Calibrated execution profile; defaults to interactive.
     * @param string|null $cursor
     * @param string|null $dialect Pinned portable SQL contract; omitted selects the current contract.
     * @param \Rafflesia\Resource\QueryRequestJoinEvidencePolicy|null $joinEvidencePolicy Automatically resolve and bind retained measurements for every complete declared SQL join. Cross-database joins default to cardinality_consistent unless allow_unverified_joins is true.
     * @param int|null $limit
     * @param string|null $ontologyDigest Optional exact ontology snapshot precondition. The request fails if this build cannot serve that digest.
     * @param array<\Rafflesia\Resource\DatabaseJoinEvidenceRequirement>|null $requiredJoinEvidence Optional exact retained join-measurement preconditions. Each row is verified against the named pinned sources before planning or execution.
     * @param array<\Rafflesia\Resource\QuerySource>|null $sources
     * @param string $sql
     * @param bool|null $strictJoins When true, coordinate-unsafe joins (unregistered pairs, missing keys, lossy or coordinate-range mappings, cross-release joins) fail the query instead of returning advisory warnings.
     * @return \Rafflesia\Resource\EnvelopeQueryExplainData
     * @throws \Rafflesia\Exception\RafflesiaException
     */
    public function explain(
        ?array $sources,
        string $sql,
        ?bool $allowUnverifiedJoins = null,
        ?\Rafflesia\Resource\QueryBudget $budget = null,
        ?\Rafflesia\Resource\QueryRequestBudgetProfile $budgetProfile = null,
        ?string $cursor = null,
        ?string $dialect = null,
        ?\Rafflesia\Resource\QueryRequestJoinEvidencePolicy $joinEvidencePolicy = null,
        ?int $limit = null,
        ?string $ontologyDigest = null,
        ?array $requiredJoinEvidence = null,
        ?bool $strictJoins = null,
        ?\Rafflesia\RequestOptions $options = null,
    ): \Rafflesia\Resource\EnvelopeQueryExplainData {
        $body = array_filter([
            'allow_unverified_joins' => $allowUnverifiedJoins,
            'budget' => $budget,
            'budget_profile' => $budgetProfile?->value,
            'cursor' => $cursor,
            'dialect' => $dialect,
            'join_evidence_policy' => $joinEvidencePolicy?->value,
            'limit' => $limit,
            'ontology_digest' => $ontologyDigest,
            'required_join_evidence' => $requiredJoinEvidence,
            'sources' => $sources,
            'sql' => $sql,
            'strict_joins' => $strictJoins,
        ], fn ($v) => $v !== null);
        $response = $this->client->request(
            method: 'POST',
            path: 'v1/query/explain',
            body: $body,
            options: $options,
        );
        return EnvelopeQueryExplainData::fromArray($response);
    }
}
