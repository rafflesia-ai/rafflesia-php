<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class HomologySearchResultSet implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        public string $canonicalResultId,
        public int $count,
        /** RFC 3339 instant after which this immutable result set is no longer retrievable through the retained-search API. */
        public \DateTimeImmutable $expiresAt,
        public bool $isResultLimitReached,
        public string $object,
        public string $sha256,
        public string $url,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'canonical_result_id',
            'count',
            'expires_at',
            'is_result_limit_reached',
            'object',
            'sha256',
            'url',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for HomologySearchResultSet::fromArray()");
            }
        }
        return new self(
            canonicalResultId: $data['canonical_result_id'],
            count: $data['count'],
            expiresAt: new \DateTimeImmutable($data['expires_at']),
            isResultLimitReached: $data['is_result_limit_reached'],
            object: $data['object'],
            sha256: $data['sha256'],
            url: $data['url'],
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'canonical_result_id' => $this->canonicalResultId,
            'count' => $this->count,
            'expires_at' => $this->expiresAt->format(\DateTimeInterface::RFC3339_EXTENDED),
            'is_result_limit_reached' => $this->isResultLimitReached,
            'object' => $this->object,
            'sha256' => $this->sha256,
            'url' => $this->url,
        ];
    }
}
