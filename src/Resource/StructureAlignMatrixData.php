<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class StructureAlignMatrixData implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Adapter identifier (usalign). */
        public string $backend,
        /** Pairs for which US-align ran and parsed. */
        public int $executedPairCount,
        /** Number of unordered pairs (n*(n-1)/2). */
        public int $pairCount,
        /** @var array<\Rafflesia\Resource\StructureAlignMatrixPair>|null */
        public ?array $pairs,
        /**
         * Deduplicated input ids, in the order compared.
         * @var array<string>|null
         */
        public ?array $structureIds,
        /** Table artifact manifest when output_table is requested */
        public ?RelationArtifactData $table = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'backend',
            'executed_pair_count',
            'pair_count',
            'pairs',
            'structure_ids',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for StructureAlignMatrixData::fromArray()");
            }
        }
        return new self(
            backend: $data['backend'],
            executedPairCount: $data['executed_pair_count'],
            pairCount: $data['pair_count'],
            pairs: isset($data['pairs']) ? array_map(fn ($item) => StructureAlignMatrixPair::fromArray($item), $data['pairs']) : null,
            structureIds: $data['structure_ids'] ?? null,
            table: isset($data['table']) ? RelationArtifactData::fromArray($data['table']) : null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'backend' => $this->backend,
            'executed_pair_count' => $this->executedPairCount,
            'pair_count' => $this->pairCount,
            'pairs' => $this->pairs !== null ? array_map(fn ($item) => $item->toArray(), $this->pairs) : null,
            'structure_ids' => $this->structureIds,
            'table' => $this->table?->toArray(),
        ];
    }
}
