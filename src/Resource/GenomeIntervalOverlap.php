<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class GenomeIntervalOverlap implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Contig of the A interval. */
        public string $aContig,
        /** 0-based half-open end of the A interval. */
        public int $aEnd,
        /** 0-based half-open start of the A interval. */
        public int $aStart,
        /** Contig of the B interval. */
        public string $bContig,
        /** 0-based half-open end of the B interval. */
        public int $bEnd,
        /** 0-based half-open start of the B interval. */
        public int $bStart,
        /** 0-based half-open end of the overlap. */
        public int $overlapEnd,
        /** Overlap length in base pairs (overlap_end-overlap_start). */
        public int $overlapLengthBp,
        /** 0-based half-open start of the overlap. */
        public int $overlapStart,
        /** Name of the A interval. */
        public ?string $aName = null,
        /** Name of the B interval. */
        public ?string $bName = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'a_contig',
            'a_end',
            'a_start',
            'b_contig',
            'b_end',
            'b_start',
            'overlap_end',
            'overlap_length_bp',
            'overlap_start',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for GenomeIntervalOverlap::fromArray()");
            }
        }
        return new self(
            aContig: $data['a_contig'],
            aEnd: $data['a_end'],
            aStart: $data['a_start'],
            bContig: $data['b_contig'],
            bEnd: $data['b_end'],
            bStart: $data['b_start'],
            overlapEnd: $data['overlap_end'],
            overlapLengthBp: $data['overlap_length_bp'],
            overlapStart: $data['overlap_start'],
            aName: $data['a_name'] ?? null,
            bName: $data['b_name'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'a_contig' => $this->aContig,
            'a_end' => $this->aEnd,
            'a_start' => $this->aStart,
            'b_contig' => $this->bContig,
            'b_end' => $this->bEnd,
            'b_start' => $this->bStart,
            'overlap_end' => $this->overlapEnd,
            'overlap_length_bp' => $this->overlapLengthBp,
            'overlap_start' => $this->overlapStart,
            'a_name' => $this->aName,
            'b_name' => $this->bName,
        ];
    }
}
