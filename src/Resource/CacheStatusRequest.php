<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class CacheStatusRequest implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Local cache directory to inspect recursively. */
        public string $targetDir,
        /** Include sampled cache file rows in the response. */
        public ?bool $includeFiles = null,
        /** Optional cache byte budget. If omitted, policy.max_bytes or the workload profile budget is used when available. */
        public ?int $maxBytes = null,
        /** Maximum cache file rows to include when include_files is true. Defaults to 100. */
        public ?int $maxFiles = null,
        /** Logical cache namespace recorded in the response. */
        public ?string $namespace = null,
        /** Optional TTL filter for eviction pressure planning. Age basis is mtime. */
        public ?int $olderThanSeconds = null,
        /**
         * Absolute paths, target-dir-relative paths, or file basenames that must not be evicted.
         * @var array<string>|null
         */
        public ?array $pinnedPaths = null,
        /** Optional policy object; top-level fields override empty policy fields. */
        public ?CacheEvictionPolicy $policy = null,
        /** Optional serverless workload profile whose cache pressure policy should be applied. */
        public ?string $workloadProfile = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'target_dir',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for CacheStatusRequest::fromArray()");
            }
        }
        return new self(
            targetDir: $data['target_dir'],
            includeFiles: $data['include_files'] ?? null,
            maxBytes: $data['max_bytes'] ?? null,
            maxFiles: $data['max_files'] ?? null,
            namespace: $data['namespace'] ?? null,
            olderThanSeconds: $data['older_than_seconds'] ?? null,
            pinnedPaths: $data['pinned_paths'] ?? null,
            policy: isset($data['policy']) ? CacheEvictionPolicy::fromArray($data['policy']) : null,
            workloadProfile: $data['workload_profile'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'target_dir' => $this->targetDir,
            'include_files' => $this->includeFiles,
            'max_bytes' => $this->maxBytes,
            'max_files' => $this->maxFiles,
            'namespace' => $this->namespace,
            'older_than_seconds' => $this->olderThanSeconds,
            'pinned_paths' => $this->pinnedPaths,
            'policy' => $this->policy?->toArray(),
            'workload_profile' => $this->workloadProfile,
        ];
    }
}
