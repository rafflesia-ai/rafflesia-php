<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class DatabaseJoinSideMeasurement implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        public int $distinctKeyCount,
        public DatabaseJoinKeyProfileRef $keyProfile,
        public int $matchedDistinctKeyCount,
        public int $matchedRowCount,
        public float $matchedRowFraction,
        public int $maxRowsPerKey,
        /** Distinct non-null keys represented by more than one row on this side. */
        public int $multirowDistinctKeyCount,
        public int $nonNullKeyRowCount,
        public int $nullKeyRowCount,
        public string $relationDigest,
        public string $releaseDigest,
        public int $rowCount,
        public string $selector,
        public int $unmatchedDistinctKeyCount,
        public int $unmatchedRowCount,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'distinct_key_count',
            'key_profile',
            'matched_distinct_key_count',
            'matched_row_count',
            'matched_row_fraction',
            'max_rows_per_key',
            'multirow_distinct_key_count',
            'non_null_key_row_count',
            'null_key_row_count',
            'relation_digest',
            'release_digest',
            'row_count',
            'selector',
            'unmatched_distinct_key_count',
            'unmatched_row_count',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for DatabaseJoinSideMeasurement::fromArray()");
            }
        }
        return new self(
            distinctKeyCount: $data['distinct_key_count'],
            keyProfile: DatabaseJoinKeyProfileRef::fromArray($data['key_profile']),
            matchedDistinctKeyCount: $data['matched_distinct_key_count'],
            matchedRowCount: $data['matched_row_count'],
            matchedRowFraction: $data['matched_row_fraction'],
            maxRowsPerKey: $data['max_rows_per_key'],
            multirowDistinctKeyCount: $data['multirow_distinct_key_count'],
            nonNullKeyRowCount: $data['non_null_key_row_count'],
            nullKeyRowCount: $data['null_key_row_count'],
            relationDigest: $data['relation_digest'],
            releaseDigest: $data['release_digest'],
            rowCount: $data['row_count'],
            selector: $data['selector'],
            unmatchedDistinctKeyCount: $data['unmatched_distinct_key_count'],
            unmatchedRowCount: $data['unmatched_row_count'],
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'distinct_key_count' => $this->distinctKeyCount,
            'key_profile' => $this->keyProfile->toArray(),
            'matched_distinct_key_count' => $this->matchedDistinctKeyCount,
            'matched_row_count' => $this->matchedRowCount,
            'matched_row_fraction' => $this->matchedRowFraction,
            'max_rows_per_key' => $this->maxRowsPerKey,
            'multirow_distinct_key_count' => $this->multirowDistinctKeyCount,
            'non_null_key_row_count' => $this->nonNullKeyRowCount,
            'null_key_row_count' => $this->nullKeyRowCount,
            'relation_digest' => $this->relationDigest,
            'release_digest' => $this->releaseDigest,
            'row_count' => $this->rowCount,
            'selector' => $this->selector,
            'unmatched_distinct_key_count' => $this->unmatchedDistinctKeyCount,
            'unmatched_row_count' => $this->unmatchedRowCount,
        ];
    }
}
