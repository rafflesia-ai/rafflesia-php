<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class MoleculeDescribeData implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Backend identifier (rdkit). */
        public string $backend,
        /** Whether the RDKit backend ran and produced descriptors. */
        public bool $computed,
        /** Echoed input SMILES. */
        public string $smiles,
        /** RDKit descriptor measurements; absent when the RDKit backend is unavailable. */
        public ?MoleculeDescriptors $descriptors = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'backend',
            'computed',
            'smiles',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for MoleculeDescribeData::fromArray()");
            }
        }
        return new self(
            backend: $data['backend'],
            computed: $data['computed'],
            smiles: $data['smiles'],
            descriptors: isset($data['descriptors']) ? MoleculeDescriptors::fromArray($data['descriptors']) : null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'backend' => $this->backend,
            'computed' => $this->computed,
            'smiles' => $this->smiles,
            'descriptors' => $this->descriptors?->toArray(),
        ];
    }
}
