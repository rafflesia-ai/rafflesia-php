<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class DatasetSourceSnapshotImportRequest implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Source database name, for example uniprot. */
        public string $source,
        /** Immutable source database version or release label. */
        public string $version,
        /** Rows per durable Parquet checkpoint part in streaming mode. Defaults to 50000. */
        public ?int $batchRows = null,
        /** Explicit streaming checkpoint JSON path. Defaults below output_dir and is never server-side query state. */
        public ?string $checkpointPath = null,
        /** Logical database name carried into the emitted records relation. Defaults to source. */
        public ?string $database = null,
        /** catalog preserves the small-catalog entry manifest; streaming reads bounded batches and is required for full UniProt releases. Defaults to catalog for compatibility. */
        public ?DatasetSourceSnapshotImportRequestImportMode $importMode = null,
        /** Object id containing FASTA bytes to import instead of input_path. */
        public ?string $inputObjectId = null,
        /** Local FASTA path to import as the source snapshot. */
        public ?string $inputPath = null,
        /** Directory for local relation artifacts. Defaults under the server data root. */
        public ?string $outputDir = null,
        /** Header parser used for alias extraction. Defaults from source. */
        public ?DatasetSourceSnapshotImportRequestParser $parser = null,
        /** Immutable release carried into the emitted records relation. Defaults to version. */
        public ?string $release = null,
        /** Resume a streaming import from its explicit checkpoint after verifying the immutable input hash and parameters. */
        public ?bool $resume = null,
        /** Input format. Defaults to fasta. */
        public string $format = 'fasta',
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'source',
            'version',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for DatasetSourceSnapshotImportRequest::fromArray()");
            }
        }
        return new self(
            source: $data['source'],
            version: $data['version'],
            batchRows: $data['batch_rows'] ?? null,
            checkpointPath: $data['checkpoint_path'] ?? null,
            database: $data['database'] ?? null,
            importMode: isset($data['import_mode']) ? DatasetSourceSnapshotImportRequestImportMode::from($data['import_mode']) : null,
            inputObjectId: $data['input_object_id'] ?? null,
            inputPath: $data['input_path'] ?? null,
            outputDir: $data['output_dir'] ?? null,
            parser: isset($data['parser']) ? DatasetSourceSnapshotImportRequestParser::from($data['parser']) : null,
            release: $data['release'] ?? null,
            resume: $data['resume'] ?? null,
            format: $data['format'] ?? 'fasta',
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'source' => $this->source,
            'version' => $this->version,
            'batch_rows' => $this->batchRows,
            'checkpoint_path' => $this->checkpointPath,
            'database' => $this->database,
            'import_mode' => $this->importMode?->value,
            'input_object_id' => $this->inputObjectId,
            'input_path' => $this->inputPath,
            'output_dir' => $this->outputDir,
            'parser' => $this->parser?->value,
            'release' => $this->release,
            'resume' => $this->resume,
            'format' => $this->format,
        ];
    }
}
