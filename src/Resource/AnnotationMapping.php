<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class AnnotationMapping implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Source annotation record being mapped. */
        public AnnotationRecord $annotation,
        /** Whether a resolved coordinate exists on the target; false when only identifier-level mapping is available locally. */
        public bool $isPositionallyMapped,
        /** Target reference the annotation is mapped onto. */
        public string $toRef,
        /**
         * Per-mapping warnings.
         * @var array<\Rafflesia\Resource\SearchWarning>|null
         */
        public ?array $warnings = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'annotation',
            'is_positionally_mapped',
            'to_ref',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for AnnotationMapping::fromArray()");
            }
        }
        return new self(
            annotation: AnnotationRecord::fromArray($data['annotation']),
            isPositionallyMapped: $data['is_positionally_mapped'],
            toRef: $data['to_ref'],
            warnings: isset($data['warnings']) ? array_map(fn ($item) => SearchWarning::fromArray($item), $data['warnings']) : null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'annotation' => $this->annotation->toArray(),
            'is_positionally_mapped' => $this->isPositionallyMapped,
            'to_ref' => $this->toRef,
            'warnings' => $this->warnings !== null ? array_map(fn ($item) => $item->toArray(), $this->warnings) : null,
        ];
    }
}
