<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class CombinedSearchData implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Combined search backend identifier. */
        public string $backend,
        /** Effective search domain: sequence, structure, or mixed. */
        public string $domain,
        /** Number of sources that failed and were either tolerated or caused failure. */
        public int $failedSourceCount,
        /** Number of merged hits returned. */
        public int $hitCount,
        /**
         * Merged, deduplicated hits with per-source provenance.
         * @var array<\Rafflesia\Resource\CombinedSearchHit>|null
         */
        public ?array $hits,
        /** Maximum merged hits requested. */
        public int $maxHits,
        /** Whether the response contains partial results due to one or more failed sources. */
        public bool $partial,
        /** Number of raw source hits before deduplication and max_hits truncation. */
        public int $rawHitCount,
        /** Number of requested sources. */
        public int $requestedSourceCount,
        /**
         * Per-source execution status and physical read evidence.
         * @var array<\Rafflesia\Resource\CombinedSearchSourceStatus>|null
         */
        public ?array $sources,
        /** Number of sources that completed successfully. */
        public int $succeededSourceCount,
        /**
         * Overlay namespaces that can be considered for frozen reference release promotion.
         * @var array<\Rafflesia\Resource\CombinedSearchPromotionCandidate>|null
         */
        public ?array $promotionCandidates = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'backend',
            'domain',
            'failed_source_count',
            'hit_count',
            'hits',
            'max_hits',
            'partial',
            'raw_hit_count',
            'requested_source_count',
            'sources',
            'succeeded_source_count',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for CombinedSearchData::fromArray()");
            }
        }
        return new self(
            backend: $data['backend'],
            domain: $data['domain'],
            failedSourceCount: $data['failed_source_count'],
            hitCount: $data['hit_count'],
            hits: isset($data['hits']) ? array_map(fn ($item) => CombinedSearchHit::fromArray($item), $data['hits']) : null,
            maxHits: $data['max_hits'],
            partial: $data['partial'],
            rawHitCount: $data['raw_hit_count'],
            requestedSourceCount: $data['requested_source_count'],
            sources: isset($data['sources']) ? array_map(fn ($item) => CombinedSearchSourceStatus::fromArray($item), $data['sources']) : null,
            succeededSourceCount: $data['succeeded_source_count'],
            promotionCandidates: isset($data['promotion_candidates']) ? array_map(fn ($item) => CombinedSearchPromotionCandidate::fromArray($item), $data['promotion_candidates']) : null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'backend' => $this->backend,
            'domain' => $this->domain,
            'failed_source_count' => $this->failedSourceCount,
            'hit_count' => $this->hitCount,
            'hits' => $this->hits !== null ? array_map(fn ($item) => $item->toArray(), $this->hits) : null,
            'max_hits' => $this->maxHits,
            'partial' => $this->partial,
            'raw_hit_count' => $this->rawHitCount,
            'requested_source_count' => $this->requestedSourceCount,
            'sources' => $this->sources !== null ? array_map(fn ($item) => $item->toArray(), $this->sources) : null,
            'succeeded_source_count' => $this->succeededSourceCount,
            'promotion_candidates' => $this->promotionCandidates !== null ? array_map(fn ($item) => $item->toArray(), $this->promotionCandidates) : null,
        ];
    }
}
