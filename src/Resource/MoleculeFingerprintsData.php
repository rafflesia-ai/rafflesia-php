<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class MoleculeFingerprintsData implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Backend identifier (rdkit). */
        public string $backend,
        /**
         * Named fingerprint measurements; empty when the RDKit backend is unavailable.
         * @var array<\Rafflesia\Resource\MoleculeFingerprint>|null
         */
        public ?array $fingerprints,
        public string $moleculeId,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'backend',
            'fingerprints',
            'molecule_id',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for MoleculeFingerprintsData::fromArray()");
            }
        }
        return new self(
            backend: $data['backend'],
            fingerprints: isset($data['fingerprints']) ? array_map(fn ($item) => MoleculeFingerprint::fromArray($item), $data['fingerprints']) : null,
            moleculeId: $data['molecule_id'],
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'backend' => $this->backend,
            'fingerprints' => $this->fingerprints !== null ? array_map(fn ($item) => $item->toArray(), $this->fingerprints) : null,
            'molecule_id' => $this->moleculeId,
        ];
    }
}
