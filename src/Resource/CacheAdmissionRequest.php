<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class CacheAdmissionRequest implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Estimated local cache bytes needed for the candidate. */
        public int $estimatedSizeBytes,
        /** Local cache directory that would receive the candidate. */
        public string $targetDir,
        /**
         * Candidate content-addressed objects that would be admitted.
         * @var array<string>|null
         */
        public ?array $candidateObjectIds = null,
        /** Caller-visible candidate ref, such as an index ref or manifest object id. */
        public ?string $candidateRef = null,
        /** Expected number of reuses during the caller's planning horizon. */
        public ?int $expectedReuseCount = null,
        /** Optional cache byte budget override. */
        public ?int $maxBytes = null,
        /** Logical cache namespace for the candidate. */
        public ?string $namespace = null,
        /** Optional TTL filter for pressure planning. */
        public ?int $olderThanSeconds = null,
        /**
         * Paths that must not be evicted while considering admission.
         * @var array<string>|null
         */
        public ?array $pinnedPaths = null,
        /** Optional cache policy object. */
        public ?CacheEvictionPolicy $policy = null,
        /** Optional caller priority. Zero means normal priority. */
        public ?int $priority = null,
        /** When true, reject admission if the candidate cannot fit without deleting eligible bytes. */
        public ?bool $requireFreeCapacity = null,
        /** Workload requesting cache admission, for example blast, blast_search, fold_index, or object_artifact. */
        public ?string $workload = null,
        /** Optional serverless workload profile whose cache policy should be applied. */
        public ?string $workloadProfile = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'estimated_size_bytes',
            'target_dir',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for CacheAdmissionRequest::fromArray()");
            }
        }
        return new self(
            estimatedSizeBytes: $data['estimated_size_bytes'],
            targetDir: $data['target_dir'],
            candidateObjectIds: $data['candidate_object_ids'] ?? null,
            candidateRef: $data['candidate_ref'] ?? null,
            expectedReuseCount: $data['expected_reuse_count'] ?? null,
            maxBytes: $data['max_bytes'] ?? null,
            namespace: $data['namespace'] ?? null,
            olderThanSeconds: $data['older_than_seconds'] ?? null,
            pinnedPaths: $data['pinned_paths'] ?? null,
            policy: isset($data['policy']) ? CacheEvictionPolicy::fromArray($data['policy']) : null,
            priority: $data['priority'] ?? null,
            requireFreeCapacity: $data['require_free_capacity'] ?? null,
            workload: $data['workload'] ?? null,
            workloadProfile: $data['workload_profile'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'estimated_size_bytes' => $this->estimatedSizeBytes,
            'target_dir' => $this->targetDir,
            'candidate_object_ids' => $this->candidateObjectIds,
            'candidate_ref' => $this->candidateRef,
            'expected_reuse_count' => $this->expectedReuseCount,
            'max_bytes' => $this->maxBytes,
            'namespace' => $this->namespace,
            'older_than_seconds' => $this->olderThanSeconds,
            'pinned_paths' => $this->pinnedPaths,
            'policy' => $this->policy?->toArray(),
            'priority' => $this->priority,
            'require_free_capacity' => $this->requireFreeCapacity,
            'workload' => $this->workload,
            'workload_profile' => $this->workloadProfile,
        ];
    }
}
