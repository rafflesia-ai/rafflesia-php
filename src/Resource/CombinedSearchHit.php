<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class CombinedSearchHit implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Hit domain: sequence or structure. */
        public string $domain,
        /** One-based position in the merged result list. */
        public int $position,
        /** Stable subject identity after source-level deduplication. */
        public string $recordId,
        /** Best source-local score among attached sources. */
        public float $score,
        /** Meaning of the selected score. */
        public string $scoreKind,
        /**
         * Reference and overlay provenances that produced this hit.
         * @var array<\Rafflesia\Resource\CombinedSearchHitSource>|null
         */
        public ?array $sources,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'domain',
            'position',
            'record_id',
            'score',
            'score_kind',
            'sources',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for CombinedSearchHit::fromArray()");
            }
        }
        return new self(
            domain: $data['domain'],
            position: $data['position'],
            recordId: $data['record_id'],
            score: $data['score'],
            scoreKind: $data['score_kind'],
            sources: isset($data['sources']) ? array_map(fn ($item) => CombinedSearchHitSource::fromArray($item), $data['sources']) : null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'domain' => $this->domain,
            'position' => $this->position,
            'record_id' => $this->recordId,
            'score' => $this->score,
            'score_kind' => $this->scoreKind,
            'sources' => $this->sources !== null ? array_map(fn ($item) => $item->toArray(), $this->sources) : null,
        ];
    }
}
