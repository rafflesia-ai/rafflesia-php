<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class StructureAlignRequest implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Query structure id, chain-scoped id (<parent>:CHAIN:<chainID>), or local path (normalized as Structure_1). */
        public string $queryId,
        /** Target structure id, chain-scoped id (<parent>:CHAIN:<chainID>), or local path (normalized as Structure_2). */
        public string $targetId,
        /** Fetch either structure from its source (PDB/AlphaFold) when it is not already cached, mirroring proteins structures fetch --allow-network. */
        public ?bool $allowNetwork = null,
        /** Optional US-align -mm multimer alignment mode; omitted leaves the US-align default. */
        public ?int $multimerMode = null,
        /** Optional US-align -ter chain/terminus handling mode; omitted leaves the US-align default. */
        public ?int $terminusMode = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'query_id',
            'target_id',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for StructureAlignRequest::fromArray()");
            }
        }
        return new self(
            queryId: $data['query_id'],
            targetId: $data['target_id'],
            allowNetwork: $data['allow_network'] ?? null,
            multimerMode: $data['multimer_mode'] ?? null,
            terminusMode: $data['terminus_mode'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'query_id' => $this->queryId,
            'target_id' => $this->targetId,
            'allow_network' => $this->allowNetwork,
            'multimer_mode' => $this->multimerMode,
            'terminus_mode' => $this->terminusMode,
        ];
    }
}
