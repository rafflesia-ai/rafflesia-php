<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class CacheEvictionPolicy implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Only evict cache files older than this many seconds when positive. */
        public ?int $maxAgeSeconds = null,
        /** If positive, evict oldest eligible files until eligible bytes are less than or equal to this value. */
        public ?int $maxBytes = null,
        /** Logical cache namespace, for example object_artifacts, blast_db, foldseek_index, or a caller-defined sweep namespace. */
        public ?string $namespace = null,
        /** If positive, only files whose modification time is at least this old are eligible. Age basis is mtime. */
        public ?int $olderThanSeconds = null,
        /**
         * Absolute paths, target-dir-relative paths, file basenames, or cache-relative paths that must not be evicted.
         * @var array<string>|null
         */
        public ?array $pinnedPaths = null,
        /**
         * Shard ids that callers want to keep; shard-aware primitives map these to paths when possible.
         * @var array<string>|null
         */
        public ?array $pinnedShards = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        return new self(
            maxAgeSeconds: $data['max_age_seconds'] ?? null,
            maxBytes: $data['max_bytes'] ?? null,
            namespace: $data['namespace'] ?? null,
            olderThanSeconds: $data['older_than_seconds'] ?? null,
            pinnedPaths: $data['pinned_paths'] ?? null,
            pinnedShards: $data['pinned_shards'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'max_age_seconds' => $this->maxAgeSeconds,
            'max_bytes' => $this->maxBytes,
            'namespace' => $this->namespace,
            'older_than_seconds' => $this->olderThanSeconds,
            'pinned_paths' => $this->pinnedPaths,
            'pinned_shards' => $this->pinnedShards,
        ];
    }
}
