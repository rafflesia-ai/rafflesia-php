<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class RunEndpointRafflesiaMsaBoltz2OutputBoltz2MsaChainData implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        public string $chainId,
        public int $pairedRowCount,
        /** Complete Boltz key,sequence CSV for this chain. Synthetic numeric keys preserve the exact deterministic cross-chain assignment; all other source rows use the unpaired sentinel. */
        public RunEndpointRafflesiaMsaBoltz2OutputMsaResourceObject $pairingCsv,
        public RunEndpointRafflesiaMsaBoltz2OutputMsaPlacementQuery $query,
        /** Rows in the taxonomy-bearing source considered for cross-chain pairing. */
        public int $sourceRowCount,
        /**
         * Pinned UniRef30 and environmental searches used to construct this chain's MSA.
         * @var array<\Rafflesia\Resource\RunEndpointRafflesiaMsaBoltz2OutputMsaResourceLane>
         */
        public array $sources,
        /** Complete query-anchored alignment retained for inspection and as a parser fallback. */
        public RunEndpointRafflesiaMsaBoltz2OutputMsaResourceArtifact $unpairedA3M,
        public int $unpairedRowCount,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'chain_id',
            'paired_row_count',
            'pairing_csv',
            'query',
            'source_row_count',
            'sources',
            'unpaired_a3m',
            'unpaired_row_count',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for RunEndpointRafflesiaMsaBoltz2OutputBoltz2MsaChainData::fromArray()");
            }
        }
        return new self(
            chainId: $data['chain_id'],
            pairedRowCount: $data['paired_row_count'],
            pairingCsv: RunEndpointRafflesiaMsaBoltz2OutputMsaResourceObject::fromArray($data['pairing_csv']),
            query: RunEndpointRafflesiaMsaBoltz2OutputMsaPlacementQuery::fromArray($data['query']),
            sourceRowCount: $data['source_row_count'],
            sources: array_map(fn ($item) => RunEndpointRafflesiaMsaBoltz2OutputMsaResourceLane::fromArray($item), $data['sources']),
            unpairedA3M: RunEndpointRafflesiaMsaBoltz2OutputMsaResourceArtifact::fromArray($data['unpaired_a3m']),
            unpairedRowCount: $data['unpaired_row_count'],
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'chain_id' => $this->chainId,
            'paired_row_count' => $this->pairedRowCount,
            'pairing_csv' => $this->pairingCsv->toArray(),
            'query' => $this->query->toArray(),
            'source_row_count' => $this->sourceRowCount,
            'sources' => array_map(fn ($item) => $item->toArray(), $this->sources),
            'unpaired_a3m' => $this->unpairedA3M->toArray(),
            'unpaired_row_count' => $this->unpairedRowCount,
        ];
    }
}
