<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class MeasureHomologyQueryContextsRequest implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Search parameters resolved identically for every release and bound into the reusable query-context identity; this measurement does not execute a search. */
        public HomologyQueryContextProbeParameters $probe,
        /** Protein sequence measured against every selected release. */
        public HomologyQuery $query,
        /**
         * Explicit immutable releases, evaluated in input order.
         * @var array<\Rafflesia\Resource\HomologyQueryContextSelection>|null
         */
        public ?array $selections,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'probe',
            'query',
            'selections',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for MeasureHomologyQueryContextsRequest::fromArray()");
            }
        }
        return new self(
            probe: HomologyQueryContextProbeParameters::fromArray($data['probe']),
            query: HomologyQuery::fromArray($data['query']),
            selections: isset($data['selections']) ? array_map(fn ($item) => HomologyQueryContextSelection::fromArray($item), $data['selections']) : null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'probe' => $this->probe->toArray(),
            'query' => $this->query->toArray(),
            'selections' => $this->selections !== null ? array_map(fn ($item) => $item->toArray(), $this->selections) : null,
        ];
    }
}
