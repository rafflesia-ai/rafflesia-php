<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class MoleculeNormalizeData implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Backend identifier (rdkit). */
        public string $backend,
        public string $moleculeId,
        /** RDKit canonical SMILES; empty when the RDKit backend is unavailable. */
        public ?string $canonicalSmiles = null,
        /** RDKit InChIKey for the canonicalized molecule; empty when the RDKit backend is unavailable. */
        public ?string $inchikey = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'backend',
            'molecule_id',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for MoleculeNormalizeData::fromArray()");
            }
        }
        return new self(
            backend: $data['backend'],
            moleculeId: $data['molecule_id'],
            canonicalSmiles: $data['canonical_smiles'] ?? null,
            inchikey: $data['inchikey'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'backend' => $this->backend,
            'molecule_id' => $this->moleculeId,
            'canonical_smiles' => $this->canonicalSmiles,
            'inchikey' => $this->inchikey,
        ];
    }
}
