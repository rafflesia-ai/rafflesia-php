<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class DatasetSourceSnapshotFetchRequest implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Artifact kind allowed by the selected source profile. */
        public string $artifactKind,
        /** Required explicit download ceiling. The fetch stops before exceeding it. */
        public int $maxBytes,
        /** Source profile name returned by datasets source-snapshots profiles. */
        public string $source,
        /** Exact HTTPS artifact URL on an allowlisted official host and path. */
        public string $url,
        /** Pinned upstream release label recorded in provenance. */
        public string $version,
        /** Expected strong HTTP entity tag from a prior preflight when the upstream publishes no checksum. Sent as If-Match; Rafflesia still computes SHA-256 as the content identity. */
        public ?string $expectedEtag = null,
        /** Expected upstream MD5 digest when that is the only checksum published by the source. Rafflesia always computes and records SHA-256 as the content identity. */
        public ?string $expectedMd5 = null,
        /** Expected SHA-256 digest, with or without the sha256: prefix. At least one expected checksum is required. */
        public ?string $expectedSha256 = null,
        /** Optional exact byte length checked before storing the artifact. */
        public ?int $expectedSizeBytes = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'artifact_kind',
            'max_bytes',
            'source',
            'url',
            'version',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for DatasetSourceSnapshotFetchRequest::fromArray()");
            }
        }
        return new self(
            artifactKind: $data['artifact_kind'],
            maxBytes: $data['max_bytes'],
            source: $data['source'],
            url: $data['url'],
            version: $data['version'],
            expectedEtag: $data['expected_etag'] ?? null,
            expectedMd5: $data['expected_md5'] ?? null,
            expectedSha256: $data['expected_sha256'] ?? null,
            expectedSizeBytes: $data['expected_size_bytes'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'artifact_kind' => $this->artifactKind,
            'max_bytes' => $this->maxBytes,
            'source' => $this->source,
            'url' => $this->url,
            'version' => $this->version,
            'expected_etag' => $this->expectedEtag,
            'expected_md5' => $this->expectedMd5,
            'expected_sha256' => $this->expectedSha256,
            'expected_size_bytes' => $this->expectedSizeBytes,
        ];
    }
}
