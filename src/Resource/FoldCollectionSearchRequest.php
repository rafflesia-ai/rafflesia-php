<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class FoldCollectionSearchRequest implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Stored structure id or local PDB/mmCIF path used as the query. */
        public string $queryId,
        /** Explicit collection routing calibration profile object id to use for planning. */
        public ?string $calibrationObjectId = null,
        /** Execute selected segment searches as deterministic worker tasks before merging results. */
        public ?bool $distributed = null,
        /** Return the deterministic read plan without fetching feature blocks. */
        public ?bool $dryRun = null,
        /** Maximum routed candidates to verify exactly per segment; defaults to limit when exact_verify is true. */
        public ?int $exactLimit = null,
        /** Run candidate-only TM-align verification inside each searched segment before merging hits. */
        public ?bool $exactVerify = null,
        /** Maximum merged fold feature hits to return; defaults to 50. */
        public ?int $limit = null,
        /** Optional hard budget for total selected feature blocks across the collection. */
        public ?int $maxBlocks = null,
        /** Optional hard budget for total local database materialization bytes. */
        public ?int $maxMaterializedBytes = null,
        /** Optional hard budget for total planned collection object bytes. */
        public ?int $maxObjectBytes = null,
        /** Optional hard budget for total planned collection object reads. */
        public ?int $maxObjectReads = null,
        /** Maximum routed collection segments to search; defaults to a bounded planner value when a routing directory is available. */
        public ?int $maxSegments = null,
        /** Maximum concurrent collection segment worker tasks when distributed execution is enabled. */
        public ?int $maxWorkers = null,
        /** Number of adjacent feature blocks to probe per segment; defaults to 1. */
        public ?int $nprobe = null,
        /** Published fold collection ref or immutable snapshot ref. */
        public ?string $ref = null,
        /** Whether distributed collection search must fail if any selected segment worker fails; defaults to true. */
        public ?bool $requireComplete = null,
        /** Require collection-level routing to choose a bounded segment subset. */
        public ?bool $requireRouting = null,
        /** Require the collection search path to materialize zero local database bytes. */
        public ?bool $requireZeroMaterialization = null,
        /** Collection fanout mode. auto selects adaptive fanout unless max_segments is explicitly set; fixed uses max_segments/default cap. */
        public ?FoldCollectionSearchRequestRoutingMode $routingMode = null,
        /**
         * Explicit published fold segment refs to search without resolving a collection ref.
         * @var array<string>|null
         */
        public ?array $segmentRefs = null,
        /** Whether to store the deterministic distributed collection search plan artifact; defaults to distributed. */
        public ?bool $storePlan = null,
        /** Adaptive routing target for cumulative positive routing-score coverage. Defaults to a conservative planner target. */
        public ?float $targetScoreCoverage = null,
        /** Use the collection's benchmark-derived routing calibration profile to fill unset routing parameters. */
        public ?bool $useCalibration = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'query_id',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for FoldCollectionSearchRequest::fromArray()");
            }
        }
        return new self(
            queryId: $data['query_id'],
            calibrationObjectId: $data['calibration_object_id'] ?? null,
            distributed: $data['distributed'] ?? null,
            dryRun: $data['dry_run'] ?? null,
            exactLimit: $data['exact_limit'] ?? null,
            exactVerify: $data['exact_verify'] ?? null,
            limit: $data['limit'] ?? null,
            maxBlocks: $data['max_blocks'] ?? null,
            maxMaterializedBytes: $data['max_materialized_bytes'] ?? null,
            maxObjectBytes: $data['max_object_bytes'] ?? null,
            maxObjectReads: $data['max_object_reads'] ?? null,
            maxSegments: $data['max_segments'] ?? null,
            maxWorkers: $data['max_workers'] ?? null,
            nprobe: $data['nprobe'] ?? null,
            ref: $data['ref'] ?? null,
            requireComplete: $data['require_complete'] ?? null,
            requireRouting: $data['require_routing'] ?? null,
            requireZeroMaterialization: $data['require_zero_materialization'] ?? null,
            routingMode: isset($data['routing_mode']) ? FoldCollectionSearchRequestRoutingMode::from($data['routing_mode']) : null,
            segmentRefs: $data['segment_refs'] ?? null,
            storePlan: $data['store_plan'] ?? null,
            targetScoreCoverage: $data['target_score_coverage'] ?? null,
            useCalibration: $data['use_calibration'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'query_id' => $this->queryId,
            'calibration_object_id' => $this->calibrationObjectId,
            'distributed' => $this->distributed,
            'dry_run' => $this->dryRun,
            'exact_limit' => $this->exactLimit,
            'exact_verify' => $this->exactVerify,
            'limit' => $this->limit,
            'max_blocks' => $this->maxBlocks,
            'max_materialized_bytes' => $this->maxMaterializedBytes,
            'max_object_bytes' => $this->maxObjectBytes,
            'max_object_reads' => $this->maxObjectReads,
            'max_segments' => $this->maxSegments,
            'max_workers' => $this->maxWorkers,
            'nprobe' => $this->nprobe,
            'ref' => $this->ref,
            'require_complete' => $this->requireComplete,
            'require_routing' => $this->requireRouting,
            'require_zero_materialization' => $this->requireZeroMaterialization,
            'routing_mode' => $this->routingMode?->value,
            'segment_refs' => $this->segmentRefs,
            'store_plan' => $this->storePlan,
            'target_score_coverage' => $this->targetScoreCoverage,
            'use_calibration' => $this->useCalibration,
        ];
    }
}
