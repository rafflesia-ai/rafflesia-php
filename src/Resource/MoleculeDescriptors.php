<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class MoleculeDescriptors implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** RDKit hydrogen-bond acceptor count. */
        public int $hbondAcceptors,
        /** RDKit hydrogen-bond donor count. */
        public int $hbondDonors,
        /** RDKit Crippen logP. */
        public float $logp,
        /** RDKit molecular weight (Da). */
        public float $molecularWeight,
        /** RDKit topological polar surface area. */
        public float $tpsa,
        /** Count of aromatic heavy atoms. Null if unavailable. */
        public ?int $aromaticAtoms = null,
        /** Aromatic proportion: aromatic_atoms / heavy_atoms. The 'AP' term in Delaney's ESOL aqueous-solubility model. Null if unavailable. */
        public ?float $aromaticFraction = null,
        /** Non-hydrogen atom count (mol.GetNumHeavyAtoms()). Null if unavailable. */
        public ?int $heavyAtoms = null,
        /** Number of rings (SSSR, mol.GetRingInfo().NumRings()). Null if unavailable. */
        public ?int $rings = null,
        /** RDKit rotatable-bond count (Lipinski.NumRotatableBonds); the descriptor Veber's rule bounds at <= 10. Null if the wrapper did not emit it. */
        public ?int $rotatableBonds = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'hbond_acceptors',
            'hbond_donors',
            'logp',
            'molecular_weight',
            'tpsa',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for MoleculeDescriptors::fromArray()");
            }
        }
        return new self(
            hbondAcceptors: $data['hbond_acceptors'],
            hbondDonors: $data['hbond_donors'],
            logp: $data['logp'],
            molecularWeight: $data['molecular_weight'],
            tpsa: $data['tpsa'],
            aromaticAtoms: $data['aromatic_atoms'] ?? null,
            aromaticFraction: $data['aromatic_fraction'] ?? null,
            heavyAtoms: $data['heavy_atoms'] ?? null,
            rings: $data['rings'] ?? null,
            rotatableBonds: $data['rotatable_bonds'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'hbond_acceptors' => $this->hbondAcceptors,
            'hbond_donors' => $this->hbondDonors,
            'logp' => $this->logp,
            'molecular_weight' => $this->molecularWeight,
            'tpsa' => $this->tpsa,
            'aromatic_atoms' => $this->aromaticAtoms,
            'aromatic_fraction' => $this->aromaticFraction,
            'heavy_atoms' => $this->heavyAtoms,
            'rings' => $this->rings,
            'rotatable_bonds' => $this->rotatableBonds,
        ];
    }
}
