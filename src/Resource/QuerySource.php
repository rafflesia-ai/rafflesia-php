<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class QuerySource implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Logical database or dataset name for a stored relation. */
        public string $database,
        /** SQL alias used by the query. */
        public string $name,
        /** Logical relation name from the relation catalog. */
        public string $relation,
        /** Immutable database or dataset release/version. */
        public string $release,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'database',
            'name',
            'relation',
            'release',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for QuerySource::fromArray()");
            }
        }
        return new self(
            database: $data['database'],
            name: $data['name'],
            relation: $data['relation'],
            release: $data['release'],
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'database' => $this->database,
            'name' => $this->name,
            'relation' => $this->relation,
            'release' => $this->release,
        ];
    }
}
