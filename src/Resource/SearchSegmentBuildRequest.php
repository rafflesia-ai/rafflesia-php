<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class SearchSegmentBuildRequest implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Algorithm family that owns this segment layout, for example blast, foldseek, or seed. */
        public string $algorithm,
        /** Logical search segment name. */
        public string $name,
        /** Immutable segment version or source snapshot. */
        public string $version,
        /** Maximum bytes per immutable block; defaults to 4 MiB. */
        public ?int $blockSizeBytes = null,
        /** Optional dataset name carried into segment provenance. */
        public ?string $datasetName = null,
        /** Optional dataset version carried into segment provenance. */
        public ?string $datasetVersion = null,
        /** Object id containing input bytes to split into immutable segment blocks. */
        public ?string $inputObjectId = null,
        /** Input file path whose bytes will be split into immutable segment blocks. */
        public ?string $inputPath = null,
        /** Sparse-index key mode; line uses the first non-empty line prefix in each block, byte_offset uses deterministic offsets. Defaults to line. */
        public ?SearchSegmentBuildRequestKeyMode $keyMode = null,
        /** Optional upstream tool name carried into the manifest. */
        public ?string $toolName = null,
        /** Optional upstream tool version carried into the manifest. */
        public ?string $toolVersion = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'algorithm',
            'name',
            'version',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for SearchSegmentBuildRequest::fromArray()");
            }
        }
        return new self(
            algorithm: $data['algorithm'],
            name: $data['name'],
            version: $data['version'],
            blockSizeBytes: $data['block_size_bytes'] ?? null,
            datasetName: $data['dataset_name'] ?? null,
            datasetVersion: $data['dataset_version'] ?? null,
            inputObjectId: $data['input_object_id'] ?? null,
            inputPath: $data['input_path'] ?? null,
            keyMode: isset($data['key_mode']) ? SearchSegmentBuildRequestKeyMode::from($data['key_mode']) : null,
            toolName: $data['tool_name'] ?? null,
            toolVersion: $data['tool_version'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'algorithm' => $this->algorithm,
            'name' => $this->name,
            'version' => $this->version,
            'block_size_bytes' => $this->blockSizeBytes,
            'dataset_name' => $this->datasetName,
            'dataset_version' => $this->datasetVersion,
            'input_object_id' => $this->inputObjectId,
            'input_path' => $this->inputPath,
            'key_mode' => $this->keyMode?->value,
            'tool_name' => $this->toolName,
            'tool_version' => $this->toolVersion,
        ];
    }
}
