<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class HomologyResponseRanking implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Whether primary_measurement is interpretable beyond this one ordering. within_query_only means the scores rank these candidates against each other and nothing else: they are not comparable across queries, carry no null model, and an unrelated sequence can produce a higher top score than a true homolog, so no score threshold is meaningful. cross_query_calibrated means a qualified calibration produced significance for every returned candidate, and significance.measurements is the field to threshold on. */
        public HomologyResponseRankingComparability $comparability,
        public HomologyResponseRankingOrder $order,
        public HomologyResponseRankingPrimaryMeasurement $primaryMeasurement,
        /** @var array<\Rafflesia\Resource\HomologyRankingComparator>|null */
        public ?array $tieBreakers,
        public string $scope = 'resolved_policy_frontier',
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'comparability',
            'order',
            'primary_measurement',
            'tie_breakers',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for HomologyResponseRanking::fromArray()");
            }
        }
        return new self(
            comparability: HomologyResponseRankingComparability::from($data['comparability']),
            order: HomologyResponseRankingOrder::from($data['order']),
            primaryMeasurement: HomologyResponseRankingPrimaryMeasurement::from($data['primary_measurement']),
            tieBreakers: isset($data['tie_breakers']) ? array_map(fn ($item) => HomologyRankingComparator::fromArray($item), $data['tie_breakers']) : null,
            scope: $data['scope'] ?? 'resolved_policy_frontier',
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'comparability' => $this->comparability->value,
            'order' => $this->order->value,
            'primary_measurement' => $this->primaryMeasurement->value,
            'tie_breakers' => $this->tieBreakers !== null ? array_map(fn ($item) => $item->toArray(), $this->tieBreakers) : null,
            'scope' => $this->scope,
        ];
    }
}
