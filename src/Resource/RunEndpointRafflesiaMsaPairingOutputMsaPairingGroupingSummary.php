<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class RunEndpointRafflesiaMsaPairingOutputMsaPairingGroupingSummary implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Groups observed across all components before the strategy and matching rules were applied. */
        public int $candidateGroupCount,
        /** Groups whose members disagree on taxonomy id, measured whether or not they were excluded. */
        public int $conflictingGroupCount,
        /** Paired rows contributed by conflicting groups, and therefore pairing different organisms. */
        public int $conflictingPairedRows,
        public int $excludedAmbiguousGroups,
        public int $excludedConflictingGroups,
        /** Rows carrying a taxonomy id whose lineage never reached the requested grouping rank. Dropped rather than grouped on their raw id. */
        public int $excludedUnresolvedRankRows,
        /** The paired block hit maximum_paired_rows and lower-ranked groups were dropped. */
        public bool $isTruncatedByRowCap,
        public int $pairedGroupCount,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'candidate_group_count',
            'conflicting_group_count',
            'conflicting_paired_rows',
            'excluded_ambiguous_groups',
            'excluded_conflicting_groups',
            'excluded_unresolved_rank_rows',
            'is_truncated_by_row_cap',
            'paired_group_count',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for RunEndpointRafflesiaMsaPairingOutputMsaPairingGroupingSummary::fromArray()");
            }
        }
        return new self(
            candidateGroupCount: $data['candidate_group_count'],
            conflictingGroupCount: $data['conflicting_group_count'],
            conflictingPairedRows: $data['conflicting_paired_rows'],
            excludedAmbiguousGroups: $data['excluded_ambiguous_groups'],
            excludedConflictingGroups: $data['excluded_conflicting_groups'],
            excludedUnresolvedRankRows: $data['excluded_unresolved_rank_rows'],
            isTruncatedByRowCap: $data['is_truncated_by_row_cap'],
            pairedGroupCount: $data['paired_group_count'],
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'candidate_group_count' => $this->candidateGroupCount,
            'conflicting_group_count' => $this->conflictingGroupCount,
            'conflicting_paired_rows' => $this->conflictingPairedRows,
            'excluded_ambiguous_groups' => $this->excludedAmbiguousGroups,
            'excluded_conflicting_groups' => $this->excludedConflictingGroups,
            'excluded_unresolved_rank_rows' => $this->excludedUnresolvedRankRows,
            'is_truncated_by_row_cap' => $this->isTruncatedByRowCap,
            'paired_group_count' => $this->pairedGroupCount,
        ];
    }
}
