<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class HomologySignificanceAvailability implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Returned candidates missing significance. */
        public int $affectedCandidateCount,
        /** Returned candidates carrying significance. */
        public int $candidateCount,
        public Requirements $requirement,
        public HomologyEvidenceAvailabilityStatus $status,
        /** Why significance is missing. no_qualified_calibration is also the unavailable_reason the release catalog publishes for the same release, so the two surfaces name this with one string. */
        public ?HomologySignificanceAvailabilityReason $reason = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'affected_candidate_count',
            'candidate_count',
            'requirement',
            'status',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for HomologySignificanceAvailability::fromArray()");
            }
        }
        return new self(
            affectedCandidateCount: $data['affected_candidate_count'],
            candidateCount: $data['candidate_count'],
            requirement: Requirements::from($data['requirement']),
            status: HomologyEvidenceAvailabilityStatus::from($data['status']),
            reason: isset($data['reason']) ? HomologySignificanceAvailabilityReason::from($data['reason']) : null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'affected_candidate_count' => $this->affectedCandidateCount,
            'candidate_count' => $this->candidateCount,
            'requirement' => $this->requirement->value,
            'status' => $this->status->value,
            'reason' => $this->reason?->value,
        ];
    }
}
