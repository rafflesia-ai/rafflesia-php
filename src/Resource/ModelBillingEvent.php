<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Rafflesia\Resource;

readonly class ModelBillingEvent implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Discount applied to this request in USD (cost_subtotal − cost_total). */
        public float $costDiscount,
        /** Amount charged after discounts in whole nano USD; the same charge as cost_total (1 USD = 1,000,000,000 nano USD). */
        public int $costEstimateNanoUsd,
        /** Cost before discounts in USD (output_units × unit_price). */
        public float $costSubtotal,
        /** Amount charged after discounts in USD (cost_subtotal − cost_discount). */
        public float $costTotal,
        /** Endpoint identifier that was used. */
        public string $endpointId,
        /** Custom billing units for this request. */
        public ?float $outputUnits,
        /** Discount percentage applied to this request (for example, 10 = 10% discount). */
        public ?float $percentDiscount,
        /** Unique identifier for the request. */
        public string $requestId,
        /** Request timestamp in ISO8601 format. */
        public \DateTimeImmutable $timestamp,
        /** Unit price for this request. */
        public ?float $unitPrice,
        /** Authentication method label. Only populated when 'auth_method' is included in expand. */
        public ?string $authMethod = null,
        /** Structured authentication method identity. Only populated when 'auth_method_structured' is included in expand. */
        public ?ModelAuthMethod $authMethodStructured = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        foreach ([
            'cost_discount',
            'cost_estimate_nano_usd',
            'cost_subtotal',
            'cost_total',
            'endpoint_id',
            'output_units',
            'percent_discount',
            'request_id',
            'timestamp',
            'unit_price',
        ] as $__required) {
            if (!array_key_exists($__required, $data)) {
                throw new \UnexpectedValueException("Missing required field '$__required' for ModelBillingEvent::fromArray()");
            }
        }
        return new self(
            costDiscount: $data['cost_discount'],
            costEstimateNanoUsd: $data['cost_estimate_nano_usd'],
            costSubtotal: $data['cost_subtotal'],
            costTotal: $data['cost_total'],
            endpointId: $data['endpoint_id'],
            outputUnits: $data['output_units'] ?? null,
            percentDiscount: $data['percent_discount'] ?? null,
            requestId: $data['request_id'],
            timestamp: new \DateTimeImmutable($data['timestamp']),
            unitPrice: $data['unit_price'] ?? null,
            authMethod: $data['auth_method'] ?? null,
            authMethodStructured: isset($data['auth_method_structured']) ? ModelAuthMethod::fromArray($data['auth_method_structured']) : null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'cost_discount' => $this->costDiscount,
            'cost_estimate_nano_usd' => $this->costEstimateNanoUsd,
            'cost_subtotal' => $this->costSubtotal,
            'cost_total' => $this->costTotal,
            'endpoint_id' => $this->endpointId,
            'output_units' => $this->outputUnits,
            'percent_discount' => $this->percentDiscount,
            'request_id' => $this->requestId,
            'timestamp' => $this->timestamp->format(\DateTimeInterface::RFC3339_EXTENDED),
            'unit_price' => $this->unitPrice,
            'auth_method' => $this->authMethod,
            'auth_method_structured' => $this->authMethodStructured?->toArray(),
        ];
    }
}
